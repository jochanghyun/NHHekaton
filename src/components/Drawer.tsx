import React, {useContext} from 'react';

import {
  View,
  ScrollView,
  Text,
  StatusBar,
  StyleSheet,
  TouchableNativeFeedback,
  Alert,
  ToastAndroid,
} from 'react-native';

import Icons from 'react-native-vector-icons/Ionicons';
import {DrawerProps} from '../type';

// const Drawer: React.FC<DrawerProps> = ({move,closeDrawer})=>{
  
// }